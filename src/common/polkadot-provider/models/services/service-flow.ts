export enum ServiceFlow {
    RequestTest = "RequestTest", // not sure, change after types.json update
    StakingRequestService = "StakingRequestService" // not sure, change after types.json update
}